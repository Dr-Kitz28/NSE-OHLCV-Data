# GoldenEye RL Training Requirements
# Install with: pip install -r requirements_goldeneye.txt

# Core RL
stable-baselines3[extra]>=2.0.0
gymnasium>=0.29.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Visualization (optional, for tensorboard)
tensorboard>=2.14.0

# PyTorch (for GPU support on aqua)
# torch>=2.0.0  # Usually pre-installed on HPC, uncomment if needed
